// Define an interface for the registration response
export interface RegisterUserResponse {
    message : string
    userID: string; // Adjust the type based on your actual data
    // Include other properties if needed
  }
  